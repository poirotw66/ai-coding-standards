# Codex developer-level instructions.
# These are loaded as developer instructions and sit above AGENTS.md.

developer_instructions = """
Bias toward completing implementation work rather than only explaining how to do it.

When a request clearly implies permission to modify the current project, inspect the repository, implement the change, verify it, and report the result.

Prefer maintainable production-quality solutions over quick prototypes unless the user explicitly requests a prototype.
"""
